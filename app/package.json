{
  "name": "fitness-plan-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local coach-only dashboard over customers/*/program.md, feedback.md, notes.md",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/inter-tight": "^5.3.0",
    "@supabase/supabase-js": "^2.116.0",
    "jspdf": "^4.2.1",
    "marked": "^13.0.3"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
