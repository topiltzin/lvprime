{
  "name": "fitness-plan-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local coach-only dashboard over customers/*/program.md, feedback.md, notes.md",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "publish-customer": "node --env-file=.env.local server/scripts/publish.js"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "@supabase/supabase-js": "^2.116.0",
    "dompurify": "^3.4.16",
    "jspdf": "^4.2.1",
    "marked": "^13.0.3"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
